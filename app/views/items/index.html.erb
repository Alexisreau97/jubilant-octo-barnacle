<h1 class="center-align">eShop</h1>

<div class="row">
<% @items.each do |item| %>
	<div class="card-container col m6 l6 ">
		<a href="<%= item_path(item.id) %>">
			<p class="card-price right-align"><%= item.price %>€</p>
			<%= image_tag item.image_url, class: "card-img responsive-img" %>
			<p class="card-title"><%= item.title %></p>
		</a>
		<br>
		<p class="card-description"><%= item.description %></p>
	</div>
<% end %>
</div>

<center class="pagination">
	<%== pagy_materialize_nav(@pagy) %>
</center>